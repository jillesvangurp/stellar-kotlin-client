version: "2.3"
services:
  stellar:
#    docker run --rm --name stellarstandalone -p "8000:8000"  stellar/quickstart:horizon-0.15 --standalone
    image: stellar/quickstart:horizon-0.15
    ports:
      - 8000:8000
    command: "--standalone"
#    command: "--testnet"
